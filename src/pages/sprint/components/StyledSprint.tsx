import styled from 'styled-components';
import { StyledMain } from '../../../styled/SharedStyles';

const StyledSprint = styled(StyledMain)`
  justify-content: center;
`;

export default StyledSprint;
